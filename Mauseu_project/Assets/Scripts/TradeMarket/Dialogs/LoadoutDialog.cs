using UnityEngine;

namespace TradeMarket.Dialogs
{
    public class LoadoutDialog : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
